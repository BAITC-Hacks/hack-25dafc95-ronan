CATALOG_MODE=fixture
CART_MODE=demo
AI_MODE=stub
PORT=8000
# DATABASE_URL=postgresql://<local-user>@127.0.0.1:5432/ekt_assistant
# If your local server requires a password, put it only in your ignored .env or local secret store.
# TEST_DATABASE_URL=postgresql://<local-user>@127.0.0.1:5432/ekt_assistant_test
# ML_CORE_URL=http://127.0.0.1:8100
# Required only for CATALOG_MODE=live; set locally, never commit values.
# EKT_API_USER=
# EKT_API_PASSWORD=
